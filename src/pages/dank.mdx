import '../styles/plain_mdx.css'
import Wiki from '../layouts/Wiki.astro'

# hii wha tis up

this is aol lasdjkf

This is pretty cool

export default function Layout(props) {
  const toc = [
    ['The Vision', '#the-vision'],
    ['The Implementation', '#the-implementation'],
  ]
  return <Wiki toc={toc}>{props.children}</Wiki>
}
